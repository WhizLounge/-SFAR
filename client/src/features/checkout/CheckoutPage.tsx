import { Typography } from "@mui/material";

export default function CheckoutPage() {
  return (
    <Typography variant="h3"> Only authorised users should be able to use this.
    </Typography>
  )
}